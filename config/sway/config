# Sway Configuration (No Scroll)

include ~/.config/minima/sway/config.d/*

# For SwayFX, uncomment the following line:
# include ~/.config/minima/sway/fx

# Autostart applications
exec swww-daemon
exec dunst
exec env QT_QPA_PLATFORMTHEME=qt6ct GTK_THEME=Breeze-Dark qs -d
exec sh -c "~/.config/hypr/getkeys.sh"
exec dbus-update-activation-environment --systemd WAYLAND_DISPLAY XDG_CURRENT_DESKTOP
exec systemctl --user start polkit-gnome
exec wl-paste --watch cliphist store
exec sh -c "~/.config/sway/set-xft-dpi"
