include ~/.config/minima/sway.conf
include ~/.config/scroll/config.d/*

# Autostart applications
exec swww-daemon
exec dunst
exec env QT_QPA_PLATFORMTHEME=qt6ct GTK_THEME=Breeze-Dark qs -d
exec sh ~/.config/hypr/getkeys.sh
exec dbus-update-activation-environment --systemd WAYLAND_DISPLAY XDG_CURRENT_DESKTOP
exec systemctl --user start polkit-gnome
exec wl-paste --watch cliphist store
exec_always --no-startup-id sh -c 'scrollmsg input type:keyboard xkb_layout "$(localectl status | sed -n "s/^\s*X11 Layout:\s*//p")"'

# Start the inhibitor and save the PID
exec systemd-inhibit --who="Sway config" \
     --why="Swaylock keybind" \
     --what=handle-power-key --mode=block sleep infinity & \
     echo $! > /tmp/.power-button-inhibitor

# Launch Discord and Spotify
exec /usr/bin/discord
exec /usr/bin/spotify-launcher
